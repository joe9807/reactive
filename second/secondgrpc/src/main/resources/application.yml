server:
  port: ${TOMCAT_SERVER_PORT}

management:
  endpoints:
    web:
      exposure:
        include: "*"

grpc:
  server:
    port: ${SERVER_PORT}

  client:
    second-service:
      address: static://localhost:6083
      negotiation-type: plaintext