server:
  port: ${TOMCAT_SERVER_PORT}

management:
  endpoints:
    web:
      exposure:
        include: "*"

grpc:
  server:
    port: ${GRPC_SERVER_PORT}

  client:
    second-service:
      address: static://${SERVER_HOST:localhost}:6082
      negotiation-type: plaintext