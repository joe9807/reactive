server:
  port: ${TOMCAT_SERVER_PORT}

management:
  endpoints:
    web:
      exposure:
        include: "*"

grpc:
  server:
    port: ${GRPC_SERVER_PORT}

  client:
    fourth-service:
      address: static://${SERVER_HOST:localhost}:6084
      negotiation-type: plaintext